<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.Goods">

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-permission-sdk-23 android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/net_config"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Goods"
        android:usesCleartextTraffic="true"
        tools:targetApi="n">
        <activity android:name=".activities.baby.LeaderDActivity"></activity>
        <activity android:name=".activities.baby.FlowActivity" />
        <activity android:name=".activities.article.TagActivity" />
        <activity android:name=".activities.baby.TiZiActivity" />
        <activity android:name=".activities.baby.TiActivity" />
        <activity android:name=".activities.article.LeaderActivity" />
        <activity android:name=".activities.article.SelectActivity" />
        <activity android:name=".activities.CollectActivity" />
        <activity android:name=".activities.RecommendActivity" />
        <activity android:name=".activities.SquareActivity" />
        <activity android:name=".activities.My_characteristicActivity" />
        <activity android:name=".activities.article.Massages_1_Activity" />
        <activity android:name=".activities.ListActivity" />
        <activity android:name=".activities.RegisterActivity" />
        <activity android:name=".activities.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>